# v9 产品替换记录

2026-09-07 按以下两份本机原件提取并交叉核对，替换 `content/catalog.json` 及首次安装种子 `app/data/catalog.json`。原文件未修改，报价工作簿与其价格、金额、客户输入均未上传仓库。

| 原件 | SHA-256 |
|---|---|
| Yumingxing_Product_Catalogue_EN_v9.pptx | `038eccbaeed61db0d46514f47b2c4e0c77672af9f059ec0adf65723d5f74048c` |
| Yumingxing_Customer_Quotation_EN_v9.xlsx | `ffa2735e0d232892a2929e62b7360c0500e7d2e3b5ac4dc2c4109aa17be885f7` |

## 发布范围

| 分类 | 可询盘产品系列 |
|---|---:|
| Kitchenware / 厨房用具 | 7 |
| Cutlery Sets / 餐具礼盒 | 2 |
| Cutlery Collections / 餐具系列 | 23 |
| Loose Cutlery / 单品餐具 | 48 |
| 合计 | 80 |

另有 Ref.094 柄纹选项，按原表“非销售参考”保留为 `Reference only` 草稿，不进入客户清单。PPT 的 82 个展示条目对应 81 个唯一 Ref，其中 Ref.112 展示银色、金色两种工艺；按工作簿保留为同一系列、两张图片，客户在询盘备注中写明工艺。

两份原件的 81 个 Ref、型号、页码及 88 张商品图片 SHA-256 均一致。图片直接提取嵌入原图，不重绘、不人工补图。35 张原图的最大边不足 600 像素；Ref.066、077 原图本身边缘不完整，详情保留了原资料的说明。

## 参数与识别规则

- 组件使用报价表的最新数据；300 行组件保留原组件编号、中英文名称、材质、尺寸和单件重量。尺寸为厘米（长×宽），重量为克/件。
- 工作簿没有每套组件数量或厚度列，两项保持空白。客户的 Request Qty 不作为组件数量。
- Ref.008/009 均为总共 24 件的礼盒，各组件在一套中的数量待确认；三种包装分别保存为带原选项编号的图片与说明块，6 个包装选项不混入组件行。
- Ref.041 存在不同材质方案，原照片标签可能与最新参数有差异，以工作簿参数为准并保留说明。Ref.058 的 16 行是四种工艺的可选组件，Ref.084 的 14 行是系列可选组件，均不表示固定套装件数。
- 15 个组件行尺寸、12 行重量、8 行材质尚未确定，保留原表待确认标记。核验状态 `Verified` 表示已核对导入与原件的对应关系，不表示这些待确认值已得到补充。
- 新询盘 SKU 为 `YMX-REF-编号`，对应 `catalogId` 中的原 Ref；型号不充当唯一键。其他历史货号保存在自定义参数中，组件及包装编号可直接回到原表对应行。
- 整批使用新的 `v9-ref-编号` 产品 ID。旧清单记录会显示产品已不可用，需重新选择，避免旧 ID 在范围或名称变化后指向错误商品。

## 后续维护

继续在 `/admin/` 调整产品、组件、图库、详情块和分类，然后发布。导入说明记录本次替换时的状态，后续后台更新不受此统计限制。

`content/catalog.json` 是线上内容来源。本次未覆盖已有的本机 SQLite 数据库。旧图片没有继续被新目录引用的，不进入 Pages 构建快照；历史版本保留在 Git 中。
